"""Server-rendered UI: Jinja2 + htmx only, no JS framework.

``templates/suggest.html`` is the suggester page. ``prototype/suggest.html`` is its static
design reference with sample data; it copies the template's CSS, theme script, top bar and
masthead verbatim - change the two together.
"""
